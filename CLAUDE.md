# 짜오짜오 — 베트남어·한국어 학습 PWA

> **정해 둔 기준은 전부 `docs/기준.md` 에 있다. 일을 시작하기 전에 그 파일부터 읽는다.**
> 기사 고르기·카드뉴스 시각·낱말 검수·소리 검수·그림 검수·목차·Qwen 쓰는 법이 다 거기 있다.
> 다른 문서와 어긋나면 `docs/기준.md` 가 맞다.

사용자는 **비전공 1인 개발자**다. 설명은 한국어로, 전문용어는 풀어서 쓴다.
호칭은 **대표님**.

## 최우선 — 일을 시키기 전에 이 차례로 묻는다

**① 셀 수 있나?** → 그러면 **기계**가 한다. AI 를 부르지 마라.
   실측: 앱 운영 스물다섯 가지 일 중 **열넷이 기계 몫**이다. 여기서 가장 많이 아낀다.

**② 아니면, 네 관문을 통과하나?** → 통과하면 **Qwen**(이 맥, 공짜)이 한다.
   - 검산할 수 있나 (사전·규칙·있는 자료로 공짜 확인)
   - 틀리면 눈에 띄나
   - **지어낼 자리가 없나** ("몇 개 채워라"는 금지)
   - 시간이 되나 (낱말 하나 1.2초)

**③ 그래도 안 되면** 제미나이(예문·문법) 또는 클로드(눈으로 보기·마지막 판정).

판정은 `python3 tools/route.py <일 이름>` 으로 확인한다. 자세한 표는 `docs/누가-할까.md`.

### Qwen 에게 맨손으로 묻지 마라
같은 일도 **근거를 쥐여 주면** 결과가 갈린다.
- "cầu 뜻이 뭐냐" → "셔틀콕" (틀림)
- "**이 사전 뜻풀이** 중 우리 뜻이 있나" → 제대로 고름

### Qwen 이 틀리는 버릇 셋 (실측 100개, 정확도 83~89%)
① 말을 잘라먹는다 (xe máy 오토바이 → '오토')
② 비슷한데 어긋난다 (bếp núc 주방 → '주부')
③ 없는 것을 지어낸다 (목록을 채우라면 255개를 만들어냈다)

## 도구 — 무엇으로 만드나
| 만드는 것 | 도구 |
|---|---|
| 그림 | **Draw Things** (FLUX schnell, **4단계**. 8단계는 시간만 두 배) |
| 소리 북부 남녀 | **edge-tts** (`tools/gen_audio.py`) |
| 소리 남부 남녀 | **v-tts** (`tools/gen_south_vtts.py`) |
| 소리 **검수** | **Whisper** (받아쓰기로 낱말과 맞대 본다). *만드는 도구가 아니다* |
| 한글 발음 | `tools/vi_kr.py` — **AI 금지**. 같은 글자면 늘 같은 결과라야 검산이 된다 |

## 조용한 실패를 조심하라 — 오늘 하루에 여섯 번 겪었다
에러 없이 **결과만 잘못 나오는** 것이 가장 위험하다. 반드시 **숫자를 확인**하라.
- 쪽 넘기기 값을 하나만 보내 사전이 5,000개에서 끊겼다
- 요청이 실패해도 조용히 멈춰 9,499개에서 끊겼다
- 위키가 `429`(그만 걸어라)를 줘 절반이 거짓 '없음'이 됐다
- 여럿을 묶어 보냈더니 하나만 답해 83%가 거짓이 됐다
- `import` 만 해도 `main()` 이 돌아 검수 안 된 낱말이 섞였다 → 모든 도구에 `if __name__` 빗장
- **`git push` 를 안 해 커밋 33개가 앱에 반영되지 않았다**

## 일을 마쳤다고 말하기 전에
1. 숫자를 확인했나 (몇 개 처리, 몇 개 실패)
2. 눈으로 봤나 (그림·카드는 실제로 열어 보라)
3. **올렸나** — `git push origin main`. **커밋만으로는 앱이 안 바뀐다**
4. 판번호를 갱신했나 — `python3 tools/stamp.py`

## 자료가 어디 있나
- `data/order.json` — 과정. 소리·그림 도구가 이 파일을 읽는다
- `data/_new_words.json` — 새 낱말 작업판 (앱에 안 실림)
- `data/_vi_words.json` — 우리 사전 47,341개 (1차 검증용, 앱에 안 실림)
- `data/_vi_def.json` — 받아 둔 사전 뜻풀이
- `data/news_days.json` — 기사·카드뉴스

## 두 파일을 동시에 고치지 마라
`news_days.json` 을 두 작업이 함께 고쳐 **나중 것이 앞 것을 덮은** 일이 있었다.
오래 도는 일을 여럿 띄울 때는 **같은 파일을 건드리는지** 먼저 보라.
