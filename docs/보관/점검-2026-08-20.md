# 출시 전 점검 (2026-08-20)

## 찾아서 고친 버그 5건

| # | 증상 | 원인 | 영향 |
|---|---|---|---|
| 1 | **오전 9시 이전 학습이 전날로 기록** | 날짜를 `toISOString()`(UTC)으로 계산. 한국은 UTC+9 | 주간 기록이 통째로 어긋남. 아침에 하는 사람은 매일 오기록 |
| 2 | **아이폰에서 소리가 안 날 수 있음** | 매번 `new Audio()`를 새로 만듦. iOS 사파리는 사용자 조작 직후가 아니면 새 Audio 재생을 막는다 | 퀴즈 자동재생이 조용히 실패 |
| 3 | **마이크가 안 꺼짐** | 녹음 후 `stream.getTracks().stop()` 누락 | 폰 상단 녹음 표시가 계속 켜짐. 배터리·프라이버시 |
| 4 | **앞 카드 녹음이 다음 카드에서 재생** | `REC.url`이 전역이고 `REC.key` 대조를 안 함 | 발음 비교인데 다른 단어를 들려줌 |
| 5 | **시크릿 모드에서 앱 전체가 멈춤** | `localStorage.setItem` 예외 미처리 | 저장 막히면 매 저장마다 예외 |

추가로, **연타 방지를 시간(800ms)으로 걸었더니 정상 진행까지 막는** 문제가 생겨
'아직 이 화면에 있는가'로 판단하도록 다시 고쳤다. (시간 기반 가드는 쓰지 말 것)

## 점검 결과

| 항목 | 결과 |
|---|---|
| 데이터 구조 (준비 3 + Day 20) | 문제 0건 |
| 대화가 그날까지 배운 단어만 쓰는가 | 위반 0건 |
| 단어 중복 | 0건 |
| 카드 전수 훑기 | 150장, 빈 카드 0 |
| **23일 전 과정 완주** | 23/23, 배지 4개, 자바스크립트 오류 0건 |
| 배포된 음성 파일 | 앱이 요청하는 것 **누락 0건** |
| 퀴즈 3종 | 듣고 고르기 · 뜻 고르기 · 회상형 모두 정상 |
| 틀린 문제 재출제 | 5문항 → 8문항 ✔ |
| 3분만 모드 | 25개 대기 → 10개 출제 ✔ |
| 간격 | 1·3·7·14·30·60일 ✔ |
| 진도 백업 왕복 | 일치 ✔ |
| 저장 실패 시 | 앱이 계속 동작 ✔ |
| 오프라인 캐시 | 등록됨 ✔ |

### 404가 70건 났지만 문제 아님
전체 조합(211종 × 2목소리 × 2속도 = 844)을 무작정 찔러본 결과다.
대화 **변형 문장 40종**은 앱이 🔊만 달고 🐢를 안 달기 때문에 느린 판을 만들지 않았다.
앱이 실제로 요청하는 파일은 **하나도 빠지지 않았다.**

## 아직 확인 못 한 것
- **실제 아이폰/안드로이드 기기에서 마이크 권한 승인 후 녹음** — 코드와 마이크 해제는 검증했으나 실기기 권한 흐름은 미확인
- 원어민이 들었을 때 음성이 자연스러운지 (기계로는 "깨졌는지"만 알 수 있다)
